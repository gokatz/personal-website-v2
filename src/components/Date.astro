---
const { date } = Astro.props;

const MonthMap = ['Jan', 'Feb', 'Mar', 'Apr', 'May', 'Jun', 'Jul', 'Aug', 'Sep', 'Oct', 'Nov', 'Dec'];

const dateObject = new Date(date);
const dateString = `${MonthMap[dateObject.getMonth()]} ${dateObject.getFullYear()}`;
---

<span>
  {dateString}
</span>