---
// Component Imports
import Home from '../layouts/Home.astro';
import TalkTile from '../components/TalkTile.astro';
import PageTitleForMobile from '../components/PageTitleForMobile.astro';

// Data Fetching: List all Markdown posts in the repo.

let allTalks = await Astro.glob('./talks/*.md');
allTalks = allTalks.sort((a, b) => new Date(b.frontmatter.date).valueOf() - new Date(a.frontmatter.date).valueOf());
---

<Home>
	<PageTitleForMobile title="Talks" />
	{
		allTalks.map((talk) => {
			return <TalkTile talk={talk} /> 
		})
	}
</Home>